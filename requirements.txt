# IMPORTANT: Depth Surge 3D requires Python 3.9-3.12 (NOT Python 3.13+)
# Python 3.13 is not yet supported due to open3d dependency in Depth-Anything V3
# See: https://github.com/Tok/depth-surge-3d/issues/11

# Core dependencies - install these first
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0

# Application dependencies
transformers>=4.56.0,<5
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0
einops>=0.7.0
easydict>=1.9

# Experimental See-Through Marigold depth backend
diffusers>=0.35.1
accelerate>=0.34.0
safetensors>=0.4.0
PyYAML>=6.0

# Depth Anything V3 dependencies
# IMPORTANT: Install with --no-build-isolation to avoid numpy recompilation issues
# pip install --no-build-isolation git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
#
# Note: xformers is optional - provides performance optimization for DA3
# Uncomment the line below if you want to install xformers (may fail on some systems):
# xformers>=0.0.26
git+https://github.com/ByteDance-Seed/Depth-Anything-3.git

# AI Upscaling: Real-ESRGAN with standalone RRDB network (no extra dependencies)
# Uses vendored architecture from ai-forever/Real-ESRGAN
# Model weights auto-download to ~/.cache/depth-surge-3d/realesrgan/
